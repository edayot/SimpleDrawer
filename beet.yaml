id: simpledrawer
name: SimpleDrawer
version: "1.8.2"
description: A datapack that add drawers in to minecraft
author: AirDox_

data_pack:
  load: [.]
resource_pack:
  load: [.]

extend: [common.yaml]

meta:
  smithed_dependencies:
    - "smithed:custom-block": "0.1.0"
    - "smithed:crafter": "0.1.0"
    - "smithed:actionbar": "0.1.0"
    - "airdox_:itemio": "0.4.1"